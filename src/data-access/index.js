const BlockchainQuery = require("./blockchain.query");
const WalletQuery = require("./wallets.query");
const TransactionQuery = require("./transactions.query");

module.exports = {
  BlockchainQuery,
  WalletQuery,
  TransactionQuery,
};
